{
  "name": "query-builder",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "pretest": "dropdb builder-test --if-exists && createdb builder-test && psql builder-test < test/seed.sql",
    "test": "mocha"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-as-promised": "^5.3.0",
    "mocha": "^2.5.3"
  },
  "dependencies": {
    "pg": "^6.0.1"
  }
}
